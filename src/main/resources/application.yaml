spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/users_db
    username: postgres
    password: root

springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  port: 8080

api-properties:
  min-age: 18